@model IEnumerable<PlanTracker.Models.Todo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DirectionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TodoName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TodoDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedtDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DirectionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TodoName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TodoDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedtDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TodoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TodoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TodoID })
        </td>
    </tr>
}

</table>
